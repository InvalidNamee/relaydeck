{
  "name": "@relaydeck/gateway",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.13.0"
  },
  "dependencies": {
    "@relaydeck/protocol": "0.1.0",
    "ws": "8.21.3"
  },
  "scripts": {
    "build": "npm run build -w @relaydeck/protocol && node --check src/server.mjs && node --check src/config.mjs && node --check src/cdp.mjs && node --check src/chrome.mjs && node --check src/state-store.mjs",
    "start": "node src/server.mjs",
    "test": "npm run build && node --test test/*.test.mjs"
  }
}
